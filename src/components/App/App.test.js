describe("<App />", () => {
  test.todo("should render correctly");
  test.todo("should render empty state if there are no ideas");
  test.todo("should render idea tiles if there are ideas");
  test.todo(
    "should dispatch an addTile action when the plus button is clicked"
  );
  test.todo("should call usePersistToLocalStorage on each render");
  test.todo(
    "should dispatch an updateOrderBy action when the order by selection changes"
  );
});

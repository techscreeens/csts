describe("<TextArea />", () => {
  test.todo('should render correctly');
  test.todo("should auto expand the text area when required if enabled");
  test.todo("should not auto expand the text area if not enabled");
  test.todo("should call onChange when text is entered");
});

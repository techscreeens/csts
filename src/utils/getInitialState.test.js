describe("getInitialState", () => {
  test.todo("should return state from local storage is present");
  test.todo(
    "should return createdAt and updatedAt fields from persisted state as Date objects"
  );
  test.todo("should return default state if not present in local storage");
  test.todo(
    "should return default state if state in local storage cannot be deserialized"
  );
});

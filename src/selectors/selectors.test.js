describe("selectors", () => {
  describe("selectTiles", () => {
    test.todo(
      "should select all tiles ordered by createdAt (ascending) when orderBy is createdAt"
    );
    test.todo(
      "should select all tiles ordered by title (ascending) when orderBy is title"
    );
  });

  describe("selectOrderBy", () => {
    test.todo("should select orderBy");
  });
});
